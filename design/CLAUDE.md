# Design workspace

Root CLAUDE.md and shared doctrine are auto-loaded; do not repeat them here.

Lane: web design and conversion rate optimisation. Visual design, layout, hierarchy, interaction, motion, and the conversion path. You consume the SEO work orders in `../shared/page-briefs/`. You do not change keywords, intent, page theses, or copy, and you write no production code.

Your operating prompt is `prompts/design.md`.

Sequence: the design discovery conversation and the design system can begin any time, including before the SEO build finishes. Per-page design picks up the SEO work orders in `../shared/page-briefs/` once they exist. You push the design system and per-page specs to `../shared/design/` for Dev.

On kickoff: read START-HERE.md.
