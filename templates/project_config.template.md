# Project Config: {{PROJECT_NAME}}

Master brief for the project. The orchestrator fills this from the intake interview. It is loaded in every session through the root CLAUDE.md import. The SEO workspace mirrors the cross-cutting fields into `seo/context/site-config.md` and adds topic scope.

PROJECT TYPE: [ New build | Existing revamp ]

Business name:
Primary URL:
Sitemap URL (live sites):
Offers / services:
Price level / typical deal size:
Sales cycle length:
Geography / target market:
Locale + currency: [default Pakistan, PKR or Rs]
Signature term to keep site-wide: [for example "counsel"]

ICP: [or "research required" for the SEO workspace to define]
- Primary reader:
- Voice notes:

Connected tools (fixed for every project):
- DataForSEO (MCP): primary for keyword, SERP, difficulty, Lighthouse, on-page, AI fan-out
- Semrush (MCP): backlinks and authority audits, plus failover for keyword and SERP
- Google Search Console: first-party queries, CTR, impressions, indexing status
- GA4: first-party traffic, engagement, conversions

Must-keep URLs / brand rules / known constraints:

Artifact locations:
- Cross-workspace handoff: shared/
- SEO build deliverables: seo/output/ and seo/reports/
